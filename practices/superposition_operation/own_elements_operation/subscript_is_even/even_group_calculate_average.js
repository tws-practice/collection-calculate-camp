'use strict';
let _ = require('lodash');
var even_group_calculate_average = function (collection) {
  // var collection_a = [1, 2, 3, 4, 5, 6, 12, 454, 324, 21, 45, 644, 34, 56, 345, 570, 8, 4, 14];
  // var collection_b = [1, 3, 5, 7, 33, 55, 31, 555, 777];
  // var collection_c = [344, 256, 55, 777, 322, 180];
  // let result = _.chain(collection_a)
  //   .sort()
  //   .filter((element, index)=>index % 2 !== 0)
  //   .filter((element)=>element % 2 === 0).value();
  // let array = _.filter(result, (element)=>element % 2 === 0);
  // let a = _.filter(result, (element)=>element >= 100);
  // let b = _.filter(result, (element)=>10 <= element < 100);
  // let c = _.filter(result, (element)=>element < 10);
  // if (array.length > 0) {
  //  
  //
  // } else if(){
  //
  // }else {
  //  
  // }
  // console.log(array);
};
module.exports = even_group_calculate_average;
